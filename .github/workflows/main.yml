name: GenerateJson

on:
  push:
    branches:
      - main

jobs:
  run-script:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v2
      - name: Run Script
        run: swift generateList.swift
